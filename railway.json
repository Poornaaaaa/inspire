{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd inspire_backend && pip install -r requirements.txt && python manage.py collectstatic --no-input"
  },
  "deploy": {
    "startCommand": "cd inspire_backend && python manage.py migrate && python manage.py seed_inspire_data && gunicorn inspire_project.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
